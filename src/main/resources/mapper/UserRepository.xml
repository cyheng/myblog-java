<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.cyheng.repository.UserRepository">


    <select id="findUserByName" resultType="User">
        SELECT
            u.username,
            u.password,
            u.id
        FROM user u
        WHERE u.username = #{username}
    </select>
    <select id="findUserById" resultType="UserDetail">
        SELECT
        u.name name,
        u.role role,
        u.introduction introduction,
        u.avatar avatar
        FROM user u
        WHERE u.id = #{id}
    </select>

</mapper>